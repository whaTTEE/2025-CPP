#include <iostream>

template <typename T>
T add(T a, T b) {
    return a + b;
}

int main() {
    std::cout << add(3, 5) << std::endl;       // int
    std::cout << add(2.5, 3.1) << std::endl;   // double
}

// 함수의 파라미터에 어떠한 타입의 아규먼트를 넣느냐에 따라서 그에 맞춰서 계산되는 것을 템플릿 함수라고 한다.
