#include <iostream>
using namespace std;

template <typename T>   // T는 나중에 우리가 정할 타입
T print_data(T data) {
    cout << "입력받은 값은 : " << data << endl;
    return data;
}

int main() {
    print_data("2wnswoo");
}
